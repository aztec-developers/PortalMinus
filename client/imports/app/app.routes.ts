// Might get to this at some point...
// Could add details for each class if clicked on, using this

import { Route } from '@angular/router';

import {SemListComponent} from './things/sem-list.component';

export const routes: Route[] = [
  { path: '', component: SemListComponent }
];
